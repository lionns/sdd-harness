{
  "name": "sdd-harness",
  "version": "0.7.0",
  "private": true,
  "description": "Specification-Driven Development harness for agent-assisted delivery.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "status": "node scripts/harness-status.mjs",
    "lint": "node scripts/harness-lint.mjs",
    "check": "npm test && node scripts/harness-status.mjs && node scripts/harness-lint.mjs",
    "init": "node scripts/harness-init.mjs"
  }
}
